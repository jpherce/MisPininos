git --version
git --help
git init --help
git config --global user.name "Juan Pablo Herce Caro"
git config --global user.name jpherce@gmail.com
# Consultar el valor de todas las opciones configurdas
git config --list
# Consultar el valor de una opcion
git config user.name
# crear un archivo nuevo
touch [nombrearchivo]
# cambiar de rama
git checkout [rama]

# Para unir archivos seguir los siguientes pasos
## Irse a la rama que se va a quedar con la modificacion
git checkout [rama]
git merge [desde la rama]

# en caso de conflicto
abrir el archivo en notepad++
revisar el/los conflictos
<<<<<< HEAD
bla bla bla
========
>>>>>> [rama]
hacer commit

git commit -a -m 'aqui va tu mensaje'



#REVISAR LOS SIGUIENTE PARA RESOLVER CONFLICTOS
tortoisemerge
winmerge **to

git commit -m 'change3'

SINTAXIS DE COMANDOS

cambio2
cambio3
cambio4-44
cambio5
cambio6
cambio7


